node_modules/
.next/
.env.local
test-results/
playwright-report/
tsconfig.tsbuildinfo
# Agent worktrees: nested full copies of this repo, including their own .env.local
.claude/

# Vercel CLI project link — machine-specific, not shared state.
.vercel

# superpowers skill scratch state — per-session, not shared.
.superpowers/

# scrollcraft skill's local build workspace (BRIEF.md, raw source stills,
# its own package.json/node_modules) and verification screenshots — dev-time
# artifacts, not shipped. The optimized assets it produced live in public/.
/scrollcraft/
/lab/
